apiVersion: v1
kind: Service
metadata:
  name: hostyourself-web
  namespace: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hostyourself-web
  ports:
    - name: http
      port: 80
      targetPort: http
